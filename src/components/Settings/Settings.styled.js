import styled from 'styled-components';

export const SettingsStyled = styled.div`
  font-size: 1.4rem;
  padding: 2rem;
  text-align: start;

  >h2 {
    font-weight: 700;
    font-size: 2.4rem;
    margin-bottom: 2rem;
  }

  p {
    margin-top: 2rem;
    font-size: 1.3rem;
  }
`