import styled from 'styled-components';

export const QuickStartStyled = styled.div`
  >p {
    position: fixed;
    top: 0px;
    left: 0px;
    z-index: 1000;
    font-size: 17px;
    background-color: rgb(0,0,0);
    box-shadow: 7px 9px 10px rgba(0,0,0,.4);
    color: white;
    padding: 10px;
    border-radius: 5px;

    >a {
      :visited, :link, :hover, :active {
        color: white;
      }
    }
  }
`

export const MetaStyles = styled.div`

`